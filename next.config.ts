import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // The exact live demo (public/demo.html) and its script are served as static
  // assets so the replica stays byte-for-byte identical to sellhi.ai.
};

export default nextConfig;
